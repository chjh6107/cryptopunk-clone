import styled from "styled-components";

const MainBlock = styled.div`
    border: 1px solid white;
`;

const Main = () => {
    return (
        <MainBlock>
            asdf
        </MainBlock>
    );
};

export default Main;
