import styled from "styled-components";

const CollectionCardBlock = styled.div`
    
`;

const CollectionCard = () =>{
    return(
        <CollectionCardBlock>asdf</CollectionCardBlock>
    )
};

export default CollectionCard;